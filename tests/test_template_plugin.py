"""Alyce 鎻掍欢鑷姩鍖栨祴璇曟ā鏉?pytest + pytest-asyncio"""import pytestimport asynciofrom plugins.template_plugin import MyPlugin@pytest.mark.asyncioasync def test_hello_command():    class DummyClient:        async def send_message(self, chat_id, text, **kwargs):            assert chat_id == 123            assert "Hello" in text            return True    class DummyMessage:        chat = type("chat", (), {"id": 123})()    plugin = MyPlugin(DummyClient())    await plugin.hello(DummyMessage(), DummyClient(), ["test"])