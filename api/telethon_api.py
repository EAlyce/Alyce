"""Telethon API 閫傞厤鍣?瀹炵幇 BaseAPI 鎺ュ彛锛屽皝瑁?Telethon 鐩稿叧鎿嶄綔"""from typing import Any, Callablefrom telethon import TelegramClient, eventsfrom .base import BaseAPIclass TelethonAPI(BaseAPI):    def __init__(self, client: TelegramClient):        self.client = client    async def send_message(self, chat_id: Any, text: str, **kwargs):        await self.client.send_message(chat_id, text, **kwargs)    async def get_me(self):        return await self.client.get_me()    async def add_handler(self, handler: Callable):        self.client.add_event_handler(handler, events.NewMessage)    async def start(self):        await self.client.start()    async def stop(self):        await self.client.disconnect()